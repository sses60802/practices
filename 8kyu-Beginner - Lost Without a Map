public class Maps {
  public static int[] map(int[] arr) {
    if(arr == null || arr.length <1)
    return arr;
    int[] args = arr;
    for(int i = 0;i < arr.length ; i++)
      args[i] = arr[i]*2;
    return args;
  }
}
